---
exclude_paths:
  - .github/
  - venv/
skip_list:
  # We should start writing changelogs at some point
  - galaxy[no-changelog]
  # We need to support 2.14 for EL9
  - meta-runtime[unsupported-version]
